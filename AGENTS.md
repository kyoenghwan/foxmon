# 폭스몬 작업 지침

이 파일은 폭스몬의 공통 작업·검증 규칙 원본이다. 사용자의 현재 명시적 지시를 우선하며, PROJECT_GUIDE.md와 하위 지침은 기술 참고로 사용한다.

## 기본 원칙

- 모든 답변과 문서를 한국어로 작성하고 이전 분석을 불필요하게 반복하지 않는다.
- 관련 코드·설정·문서·호출 관계·데이터 흐름을 확인한 뒤 필요한 최소 범위만 변경한다.
- 기존 기능을 임의 삭제·축소하거나 요청 밖의 대규모 리팩터링을 하지 않는다.
- 오류의 근본 원인을 추적한다. fallback, retry, delay, mock, 임의 default, 강제 refresh로 증상을 덮지 않는다.
- 기존 공통 기능을 재사용하고 명확성·유지보수성을 우선한다.
- 비밀값·비밀번호·인증 토큰·개인정보를 코드·문서·로그에 기록하지 않는다. 테스트 계정은 승인된 별도 경로에서 제공받는다.
- 사용자의 기존 미커밋 변경을 보존한다. 커밋·push·운영 DB 변경·배포는 각각 사용자 요청 범위에서만 수행한다. 코드 수정 권한은 배포 권한이 아니다.
- 임시·미사용으로 보이는 코드나 데이터도 호출자·사용 여부·삭제 영향을 확인하기 전 제거하지 않는다.

## 검증 LEVEL

작업 시작 시 실패 영향도, 데이터 위험, 변경 범위, 복구 난이도로 자동 판단한다. 사용자에게 선택을 묻지 않는다. 작업 중 위험 증가 시 1 → 2 → 3으로 승격하고 임의로 하향하지 않는다.

- LEVEL 1: 문구·색상·간격·작은 UI·단순 타입/문법 수정. 기존 구조 확인 → 최소 변경 → 기본 검증 → 완료. 여러 파일·공통 로직·데이터 흐름 영향 발견 시 LEVEL 2.
- LEVEL 2: 신규 기능·API·상태·조회/저장·공통 함수·여러 파일·일반 버그. 사전 분석 → 원인/가설 확인 → 최소 구현 → 자기반박 → 테스트 → 판정. 원인 오판, 다른 원인, 정상 기능 회귀, 중복, 임시방편, 예외 입력, 데이터 중복/손실, 실패 후 잘못된 상태를 재검증한다.
- LEVEL 3: DB schema/migration, 실제 데이터 변경, 인증/권한/보안, 개인정보, 결제, 계정, 핵심 모델/API 계약, 운영 장애, 반복 재발, 대규모 리팩터링, 배포 핵심 변경·오픈 전 핵심 검증. rebuttal-verification 스킬에 따라 독립 PRIMARY와 REVIEWER가 반박검증한다. PRIMARY는 REVIEWER 의견도 직접 재확인한다. 스킬이나 독립 REVIEWER를 실제 사용할 수 없으면 가상 검증 없이 "LEVEL 3 검증 미완료"를 명시한다.

증거 우선순위: 실행 실측 → 테스트 → DB/로그/네트워크 → 실제 코드 → 공식 문서 → 논리 → 추측. AI 간 합의는 실행 증거가 아니다.

변경 범위에 맞는 lint·typecheck·build·test·실제 기능·로그·데이터를 가능한 범위에서 검증한다. 미실행 항목은 미검증으로 보고한다. 동일 기준에서 통과한 검사는 새 변경·실패·미해결 우려 없이 반복하지 않는다. test 이름의 스크립트도 DB 쓰기 여부를 읽고 실행한다.

DB 삭제/대량 변경 전 환경, 대상 조건, 영향 행 수, 변경 전 상태, rollback과 복구 가능한 backup을 확인한다. 확인되지 않으면 파괴적 작업을 수행하지 않는다.

## 지침과 스킬 라우팅

LIFORY에서 가져온 것은 역할 분리·문서 단일 관리·인수인계 방식이다. LIFORY 전용 제품 모델·Vite 경로·LEVEL 폐지 규칙은 적용하지 않는다.

관련 작업 전에 아래 프로젝트 내 SKILL.md를 읽는다. 경로는 폭스몬 루트 기준이며, 전역 설치나 앱 자동 등록에 의존하지 않는다.

| 작업 | 읽을 스킬 |
|---|---|
| Atom·비즈니스 흐름·데이터 쓰기 | agent-kit/skills/foxmon-atom-design/SKILL.md |
| UI·배너·모바일·이미지 | agent-kit/skills/foxmon-design-system/SKILL.md |
| 페이지/Atom 색인·DB 문서 | agent-kit/skills/foxmon-project-index/SKILL.md |
| 다른 PC·이전 작업 이어가기 | agent-kit/skills/foxmon-work-continuity/SKILL.md |

작업 절차는 이 파일, 기술 구조는 PROJECT_GUIDE.md, UI 특수 제약은 docs/project_rules를 따른다. .rules와 기존 템플릿의 자동 push, 점수 기반 승인 대기, 모든 입력 로깅, 형식적인 rollbackData 강제는 적용하지 않는다. 기술 제약을 완화할 필요가 생기면 영향과 사용자 요청을 먼저 확인한다.

## 완료 보고

검증 LEVEL, 변경 내용, 변경 파일, 실제 테스트/검증, 남은 문제를 짧게 보고한다. LEVEL 2 이상은 필요에 따라 검증됨·참조·미검증·후속작업으로 구분한다. LEVEL 3은 스킬의 Ledger/Checkpoint/판정 기록을 남기고 상세 문서로 연결한다.
